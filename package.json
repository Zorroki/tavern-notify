{
  "name": "sillytavern-tavern-notify",
  "version": "0.2.0",
  "description": "SillyTavern background generation plugin with Bark and Web Push notifications.",
  "type": "module",
  "main": "server/index.mjs",
  "scripts": {
    "check": "node --check server/index.mjs && node --check index.js && node --check server/sw.js"
  },
  "license": "MIT",
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
